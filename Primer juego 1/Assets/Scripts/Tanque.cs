using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Tanque : MonoBehaviour
{
    /*public float Velocidad = 10f;
    
    void Update()
    {
        Tank();


    }
    void Tank()
    {
      float hor = Input.GetAxis("Horizontal");
      float ver = Input.GetAxis("Vertical");
      float saltar = Input.GetAxis("Jump");
      Vector3 MovimientoTanque = new Vector3(hor,saltar,ver);
      transform.Translate(MovimientoTanque * Velocidad * Time.deltaTime);
 
    
    }*/
   
    
}
